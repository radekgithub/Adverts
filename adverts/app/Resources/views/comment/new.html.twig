{% extends 'base.html.twig' %}

{% block body %}
    <h3>Add Comment</h3>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-primary" value="Add" />
    {{ form_end(form) }}

    <ul class="list-group">
        <li class="list-group-item">
            <a href="{{ path('advert_show', {'id': advert.id}) }}">Back to Ad</a>
        </li>
    </ul>
{% endblock %}
