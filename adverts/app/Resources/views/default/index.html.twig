{% extends 'base.html.twig' %}

{% block body %}
    <div id="home-container">
        <h3>THESE ARE</h3>
        <h1>ADVERTS</h1>
        {% if not is_granted("ROLE_USER") %}
        <p><a href="{{ path('fos_user_security_login') }}">Log in</a> or <a href="{{ path('fos_user_registration_register') }}">register</a> an account to start posting your own ads</p>
        <p>or just browse the <a href="{{ path('category_index') }}">categories</a></p>
        {% endif %}
    </div>
{% endblock %}
